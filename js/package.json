
{
	"name": "phonomask",

	"version": "0.1.0",

	"type": "module", 

	"main": "server.js",

	"scripts":
	{
		"dev": "node server.js",
		"start": "node server.js",
		"dkr-build": "docker build -t phonomask-build .",
		"dkr-run": "docker run -p 3000:3000 phonomask-build"
	},

	"dependencies":
	{
		"aws-sdk": "^2.1693.0",
		"express": "^5.2.1",
		"uuid": "^13.0.0"
	},

	"engines":
	{
		"node": ">=14.x"
	}
}

